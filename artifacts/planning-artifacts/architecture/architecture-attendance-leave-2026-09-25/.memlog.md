---
scope: Attendance & Leave module across fenzit-be (backend) and fenzo-app (mobile)
purpose: build-substrate
altitude: feature
updated: 2026-09-25T14:10
---

- (event) Run started. Fast path, spine only. Sources: prd-Fenzo-attendance-2026-09-25 (prd.md, addendum.md, .memlog.md), ux-Fenzo-2026-09-25-attendance-leave (DESIGN.md, EXPERIENCE.md, .decision-log.md), fenzit-be project-context.md, address-autosuggest spine as format precedent
- (version) react-native-maps latest 1.29.8 (npm, Sep 2026); 1.29.0 (2026-06-28) added iOS Fabric for GoogleMaps Marker/Polygon. Compatibility with RN 0.87.1 NOT confirmed on web -> spike before office-picker story. Circle is not draggable; pin = draggable Marker, radius = Circle bound to state
- (version) @react-native-community/netinfo latest 12.0.1 (npm). fenzo-app: RN ^0.87.1, React 19.2.3, react-native-nitro-geolocation ^1.4.3, @supabase/supabase-js ^2.116.0
- (version) btree_gist available on Supabase via CREATE EXTENSION; daterange + EXCLUDE USING gist confirmed standard Postgres
- (constraint) PRD prd-Fenzo-attendance-2026-09-25 (final, FR-1..FR-28, NFR-1..NFR-12) and UX DESIGN/EXPERIENCE (final) are settled inputs; spine inherits their rules and does not re-decide them
- (constraint) Deferred security items (RPC EXECUTE grants to PUBLIC on job RPCs; users_update_own no column limit) must be fixed before FR-27 technician realtime token ships (deferred-work.md 2026-09-25)
- (constraint) Brownfield reality (code sweep 2026-09-25): services call Supabase directly (AR-2 drift), createAdmin() service-role is the dominant path with explicit tenant_id filters, RLS second layer with auth.jwt()->>'tenantId' policies; 6 SECURITY DEFINER RPCs; PTxxx SQLSTATE mapping; IdempotencyInterceptor opt-in racy; pg_cron in use; no scheduler lib; notifications table + broadcast trigger + pushed_at; no push; single Render instance; Jest 30; Bun 1.4.0; no DATE columns; no PostGIS; Role = owner|technician
- (decision) USER: all attendance/leave multi-row writes are SECURITY DEFINER RPCs, service-role only, EXECUTE revoked from PUBLIC/anon/authenticated (report-RPC grant pattern from migration 20260920000005). Conscious exception to 2026-09-20 'keep stored procedures to a minimum'. Binds all attendance writes; prevents split atomicity between state/audit/notification
- (decision) Paradigm: inherited modular monolith; attendance is a DB-centric domain core — rules in Postgres (RPCs + read functions), NestJS AttendanceModule is thin shell (auth, DTO validation, outcome->HTTP mapping, camelCase mapping). One module src/attendance/ covers attendance+leave+offices+holidays because they share the per-employee lock and day-status engine [ASSUMPTION]
- (decision) Check-in/out RPCs return an outcome enum and never RAISE on rejection so the attempt row + fake-GPS alert commit; genuine state conflicts use PTxxx SQLSTATEs
- (decision) Concurrency: pg_advisory_xact_lock on employee key in every per-employee RPC; tenant key for tenant-wide RPCs; lock order tenant then employee
- (decision) Idempotency for check-in/out, leave apply, on-behalf leave: X-Idempotency-Key required, passed as p_request_id, UNIQUE(tenant_id, request_id) on the written row, replay returns original result; IdempotencyInterceptor NOT applied to attendance routes (avoid second racy layer) [ASSUMPTION]. Other transitions are naturally idempotent via state guards
- (decision) Time: tenants.timezone IANA + CHECK; work_date DATE is the calendar key; single SQL source attendance_today(tenant); office times TIME local; timestamps timestamptz; API returns dates YYYY-MM-DD and timestamps ISO-8601 with tenant offset; client formats wall-clock from the string, no tz conversion; attendance TS never uses getIstDayRange
- (decision) Effective-dated rules (enrolment incl. office assignment, office rules, tenant weekly off, employee weekly off override) as daterange [from,to) with EXCLUDE USING gist (btree_gist); change = close current + insert new inside RPC; pin/radius live on office row [ASSUMPTION: PRD only effective-dates timing/hours]
- (decision) Snapshot on write: attendance_records store office_id, office_rules_id, expected_start/end, late_minutes, early flag, distance_m, accuracy_m, lat/lng, mocked, provider, server timestamps
- (decision) Day status computed on read by ONE SQL function over (tenant, employees, date range); dashboard, monthly summary, calendars all derive from it; no stored/materialized status, no nightly finalisation job
- (decision) Leave: leave_requests + leave_request_days (one row per calendar date in range, incl. off days); partial unique (employee_id, date) where state in (pending, approved); request-level status derived from day states, not stored; leave_events append-only audit; invalid transitions rejected by trigger guard
- (decision) Corrections: attendance_day_overrides (unique employee+date, current value) + attendance_corrections append-only audit; originals never modified; override wins
- (decision) Notifications: RPCs insert notifications rows in-transaction; additive columns entity_type, entity_id, dedupe_key (partial unique); event types namespaced attendance.* / leave.*; push wave is a property of the event-type registry, not a column [ASSUMPTION]; payload self-contained camelCase
- (decision) Reminders: pg_cron every 5 min -> SQL function computing due reminders per tenant timezone, insert with dedupe_key ON CONFLICT DO NOTHING; no in-process worker; reminders may lag up to 5 min
- (decision) Rate limit for check-in: counted from attendance_attempts inside check-in RPC under the employee lock (5 rejected/10 min -> 10 min block); replaces addendum suggestion of reusing in-memory Places rate limiter (restart/multi-instance safe, single source)
- (decision) Reads: NestJS reads via admin client with explicit tenant filter (+ user filter for technician) per existing convention, or via read SQL functions that verify p_tenant_id/p_employee_id; technician employee id always from JWT, never body; RLS SELECT-only policies, no write policies on new tables
- (decision) Access state: attendanceAccess none|upcoming|active|history_only + attendanceStartDate computed by one SQL function, exposed on /users/me; frontend navigation derives only from it
- (decision) Frontend: src/features/attendance/ own stack; existing useSyncExternalStore store pattern, no new state lib; no optimistic updates for check-in/out or leave actions; new attendance-only location capture (fresh high-accuracy fix, mocked/provider/fixAgeMs); notifications event-type registry + role-agnostic realtime bridge; job behaviour unchanged
- (decision) Routes: /api/v1/attendance/* for everything (offices, enrolments, me, leave, holidays, weekly-offs, corrections, dashboard, monthly); technician self routes under /attendance/me/*; error codes prefixed ATTENDANCE_/LEAVE_
- (decision) Release order: fenzit-be additive migrations first; deferred security hardening (RPC grants, users_update_own) merged before technician realtime token opens; then fenzo-app
- (assumption) Server rejects fixes with fixAgeMs > 30000 (defence in depth; device-reported)
- (assumption) Validation thresholds (100 m accuracy, rate limit 5/10min/10min, radius 50-1000) live as named constants in SQL, mirrored as TS constants for DTO validation, changed only by migration
- (question) NFR-9 reminder job metrics: how pg_cron run results reach OTel (read cron.job_run_details from NestJS gauge vs run-log table) - deferred to reminders story
- (event) Reviewer gate ran: reconcile-inputs, rubric, tech-currency, adversarial (reviews/). Applying fixes below
- (decision) EVERY function from attendance migrations (write, read, helper, trigger, cron entry) is SECURITY DEFINER with EXECUTE revoked from PUBLIC/anon/authenticated; RLS spec asserts via pg_proc prefix scan (fixes adversarial H-1 critical)
- (decision) attendance_day_context() internal helper is the only source of per-employee-date facts (tracked incl setup-complete + enable-day grace, office, rules, midpoint truncated to minute, weekly off override replaces default, holiday, leave day, expected start/end) for day status, check-in/out, leave RPCs, reminders, access, dashboard
- (decision) leave_transition_days() internal helper is the sole writer of leave_request_days state; causes apply|apply_on_behalf|approve|reject|employee_cancel|owner_revoke|checkin_auto_cancel|disable|removal; transitions all rows incl off days; one leave_events row; cause->notification map; system actor for disable/removal
- (decision) Enrolment split: attendance_enrolments(valid, enabled_at) + attendance_office_assignments(office_id, valid); change algorithm effective_from=max(p_from,today) (tomorrow if checked in today) -> delete ranges starting >= effective_from, cap covering range, insert; CHECK not isempty; disable truncates both; same algorithm for office rules and weekly offs
- (decision) Locks: employee RPCs take shared tenant lock then exclusive employee lock; tenant-wide RPCs take exclusive tenant lock; keys only via attendance_lock_tenant()/attendance_lock_employee()
- (decision) Snapshot office_rules_id + location only; Late/Early computed on read in day status from snapshotted rules + date context (supersedes AD-9 draft freezing flags)
- (decision) Attempts idempotent: request_id UNIQUE on attendance_attempts; replay returns stored outcome; counted outcomes too_far|low_accuracy|mocked|stale_fix; rate_limited recorded not counted; blocked_until = 5th counted rejection within 10 min + 10 min; one budget shared by check-in and check-out
- (decision) tenants.timezone validated by BEFORE INSERT/UPDATE trigger against pg_timezone_names (region names only), not CHECK
- (decision) Server preview read functions: leave_preview (working-day count, validation), leave_action_preview (revoke/cancel split), attendance_holiday_impact, office archive blockers; check-in takes p_confirm_leave_cancel with outcome leave_confirmation_required; leave request status derived in SQL read fn and returned as status
- (decision) Offices archived via archived_at + RPC under tenant lock blocked by current or future assignments of non-removed employees; holidays UNIQUE(tenant_id, holiday_date)
- (decision) Setup: wizard writes real rows; attendance_settings.setup_completed_at gates everything (access none for technicians, no reminders/notifications) until set; onboarding completion stored server-side in attendance_onboarding(employee_id, onboarded_at)
- (decision) Access: light GET /attendance/me/access for refetch; /users/me carries same fields for first load by calling attendance_access via admin rpc (no TS import of attendance)
- (decision) Owner RLS SELECT policies also require auth.jwt()->>'role' = 'owner'
- (decision) Technician realtime: the two deferred security fixes become a prerequisite story inside this initiative, merged before fenzo-app attendance ships; no polling fallback is built (resolves FR-27 real-time contradiction)
- (decision) Notification contract: backend registry (src/attendance/notification-events.ts) is source of truth for eventType, payload shape, push wave, entity type; NotificationResponse adds entityType/entityId; dedupe formats reminder:<type>:<recipient>:<date>[:<office>] and fake_location:<employee>:<yyyy-mm>
- (decision) Reminder function isolates per-tenant failures (sub-block EXCEPTION), daily prune of cron.job_run_details > 7 days; reminders suppressed only by approved full-day leave [ASSUMPTION]
- (decision) Location capture: nitro 1.4.3 pinned, no major upgrade; accuracy {android:high, ios:best}, maximumAge 0, timeout 15000; mocked boolean|null (null = not detected), iOS detection best-effort; provider informational; generalise features/technicianApp/geolocation.ts into services/location
- (decision) Disable cancels pending + future approved leave via cause=disable and notifies the employee [ASSUMPTION]; corrections on untracked dates rejected PT422; fake-location day flag cleared by owner acknowledge (acknowledged_at) [ASSUMPTION]
- (decision) Ops: kill switch = attendance_settings.enabled per tenant; migrations forward-only additive; rejected-attempt coordinates pruned after 90 days by pg_cron, record coordinates kept with the record [ASSUMPTION]; product not live so no old-build compatibility
- (decision) Stack table shows floors + resolved versions; no major upgrades in this feature (NestJS stays 11, nitro stays 1.4.3); fenzo-app Jest 29
- (decision) react-native-maps spike pass criteria: default-pin draggable Marker onDragEnd, live Circle radius/centre on Apple+Google, MapView.onPress moves pin; tap-to-place primary; no custom marker children
- (decision) USER approved all 7 assumptions (one module; pin/radius not effective-dated; only approved full-day leave suppresses check-in reminder; disable cancels pending+future leave with notification; fake-location flag until owner acknowledges; rejected-attempt coordinates nulled after 90 days; thresholds as SQL constants) and the AD-18 security prerequisite in scope; PRD + addendum + deferred-work.md updated to match
- (event) spine finalized
