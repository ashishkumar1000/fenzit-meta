---
topic: Fenzo owner Home — Today's jobs section redesign
updated: 2026-09-05T15:01
---

- (decision) Section purpose = Dispatch view + Action hub — owner scans it to know who goes where and is pushed to act (assign/nudge/progress); every card leads to an action.
- (decision) Scope = Today + overdue preview — section becomes 'Today & needs attention': today's jobs plus a compact overdue strip.
- (decision) Data depth = Full card + BE enrich — embed customer/technician names and activity/attachment counts into /users/me profile jobs rows; cards show time, service, location, status, urgent marker, technician avatar.
- (assumption) Design inherits the Fenzit Design System (src/theme/DESIGN_SYSTEM.md) — spines document only the section delta, not the whole system.
- (assumption) Mobile-only form factor (React Native app, owner persona); day logic is IST per backend convention.
- (event) Spines drafted — DESIGN.md (section delta) + EXPERIENCE.md (behaviour + data contract) written to workspace.
- (change) BE enrich narrowed to names-only embed — activity/attachment counts deliberately excluded (nothing on the section renders them); jobsScope=today query param replaces client-side filtering.
- (event) Key flows captured — morning dispatch (Kamlesh, overdue strip → jobs tab, assign-technician footer) and between-jobs refresh.
- (event) Key-screen mock rendered — mockups/key-home-todays-jobs.html (canonical, unassigned-variant, empty-state columns); spine-only surfaces: OverdueStrip-alone state, error banner (existing pattern).
- (event) Reviewer gate skipped by user; spines finalized.
- (change) Unassigned variant DROPPED — DB has technician_id NOT NULL (20260621000002_create_jobs.sql:12), unassigned jobs cannot exist. Today cards always render avatar + technician name; 'Assign technician' footer removed from contracts, mock, and stories. User decision 2026-09-05.
